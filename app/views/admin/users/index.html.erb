<%= render 'base' %>
<div id="admin_users">
  <table class="table table-striped table-bordered table-condensed">
    <thead>
      <tr>
        <th>编号</th>
        <th><%= t("mongoid.attributes.user.email") %></th>
        <th><%= t("mongoid.attributes.user.name") %></th>
        <th><%= t("mongoid.attributes.user.verified") %></th>
        <th>创建时间</th>
        <th class="opts"></th>
      </tr>
    </thead>
    <tbody>
    <% @users.each do |item| %>
    <tr class="<%= cycle("","even") %>">
        <td><%= item.id %></td>
        <td><%= item.email %></td>
        <td><%= item.name %></td>
        <td><%= item.verified %></td>
        <td><%= l(item.created_at, :format => :long) %></td>
        <td>
          <%= link_to "", edit_admin_user_path(item.id), :class => "icon-edit" %>
          <%= link_to '', admin_user_path(item.id), :method => :delete, :'data-confirm' => '确定要删除吗?', :class => "icon-remove" %>
        </td>
      </tr>
    <% end %>
    <tbody>
  </table>
  <%= will_paginate @users %>
</div>

