<%= render 'base' %>
<div id="admin_homeland_nodes">
  <table class="table table-striped table-bordered table-condensed">
    <thead>
      <tr>
        <th>编号</th>
        <th><%= t("mongoid.attributes.node.section_id") %></th>
        <th><%= t("mongoid.attributes.node.name") %></th>
        <th><%= t("mongoid.attributes.node.sort") %></th>
        <th>创建时间</th>
        <th class="opts"></th>
      </tr>
    </thead>
    <tbody>
    <% @nodes.each do |item| %>
    <tr class="<%= cycle("","even") %>">
        <td><%= item.id %></td>
        <td><%= item.section_id %></td>
        <td><%= item.name %></td>
        <td><%= item.sort %></td>
        <td><%= l(item.created_at, :format => :long) %></td>
        <td>
          <%= link_to "", edit_admin_homeland_node_path(item.id), :class => "icon-edit" %>
          <%= link_to '', admin_homeland_node_path(item.id), :method => :delete, :'data-confirm' => '确定要删除吗?', :class => "icon-remove" %>
        </td>
      </tr>
    <% end %>
    <tbody>
  </table>
  <%= will_paginate @nodes %>
</div>

