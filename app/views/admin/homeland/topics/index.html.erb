<%= render 'base' %>
<div id="admin_homeland_topics">
  <table class="table table-striped table-bordered table-condensed">
    <thead>
      <tr>
        <th>编号</th>
        <th><%= t("mongoid.attributes.topic.title") %></th>
        <th><%= t("mongoid.attributes.topic.user_id") %></th>
        <th>创建时间</th>
        <th class="opts"></th>
      </tr>
    </thead>
    <tbody>
    <% @topics.each do |item| %>
    <tr class="<%= cycle("","even") %>">
        <td><%= item.id %></td>
        <td><%= item.title %></td>
        <td><%= item.user_id %></td>
        <td><%= l(item.created_at, :format => :long) %></td>
        <td>
          <%= link_to "", edit_admin_homeland_topic_path(item.id), :class => "icon-edit" %>
          <%= link_to '', admin_homeland_topic_path(item.id), :method => :delete, :'data-confirm' => '确定要删除吗?', :class => "icon-remove" %>
        </td>
      </tr>
    <% end %>
    <tbody>
  </table>
  <%= will_paginate @topics %>
</div>

